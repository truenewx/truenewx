#root-repo
spring:
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sample_root?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: data
    password: datapwd
  data:
    mongodb:
      host: localhost
      port: 27017
      database: tnxsample
      username: data
      password: datapwd
